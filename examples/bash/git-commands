
# display your username and email together, along with other info
git config --list 
git config -l


# edit the whole config file in vi/command line editor
git config --global --edit
git config --global -e

git config -e (to edit local config)

git status
git fetch
git checkout -b dev-branch
git checkout master
git branch -d dev-branch
git checkout -b dev-branch